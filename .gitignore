# .gitignore cho project web tĩnh - Team Spirit / TheBaseWeb
# Bỏ qua các file hệ thống, cache, thư mục node, file môi trường và artefact build

# Hệ điều hành
.DS_Store
Thumbs.db
ehthumbs.db

# IDE / editors
.vscode/
.idea/

# Node (nếu có sử dụng node / npm/yarn locally)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build / cache
dist/
build/
.sass-cache/
.cache/

# Log / tạm
*.log
*.tmp

# Các file môi trường (có thể chứa secret) - không commit
.env
.env.local
.env.*.local

# Tệp CSS map và CSS biên dịch (bỏ nếu bạn muốn track các file .css đã compile)
public/css/**/*.css.map
public/css/**/style.css

# Mac specific
.Spotlight-V100
.AppleDouble
.LSOverride

# Windows specific
Desktop.ini
